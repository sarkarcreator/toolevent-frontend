# Frontend
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Next.js rewrite target for the separate backend
BACKEND_API_URL="http://localhost:3001"

# Keep frontend API client compatible with /api proxy routes
NEXT_PUBLIC_API_URL="/api"
